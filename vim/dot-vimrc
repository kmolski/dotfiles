" Color scheme
colorscheme desert

" Syntax highlighting
syntax on

" Indentation
set autoindent
filetype plugin indent on

" Line numbers on the side
set number

" Four space indents for most files
set shiftwidth=4 " Make indents (<<, >>) use 4 spaces
set tabstop=4    " Tabs displayed as 4 spaces
set expandtab    " Insert 4 spaces instead of a tab

" For Markdown and YAML, prefer 2 space indents
autocmd Filetype markdown setlocal shiftwidth=2 tabstop=2 expandtab
autocmd Filetype yaml setlocal shiftwidth=2 tabstop=2 expandtab

" Text selection with mouse
set mouse=a

" Search as pattern is entered
set incsearch

" Enable folding
set foldenable
set foldlevelstart=10

" Global replace by default
set gdefault

" Interactive search and replace
set inccommand=nosplit
